<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.novikova.opencv">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="MatrixActivity"/>
        <activity android:name="BorderActivity"/>
        <activity android:name="LoadImageActivity"/>
        <activity android:name="ShowImagesActivity"/>
        <activity android:name="DrawFigureActivity"/>
        <activity android:name="TextActivity"/>
        <activity android:name="ContourActivity"/>
        <activity android:name=".AffineDeformationActivity"/>
        <activity android:name=".ResizeImageActivity"/>
        <activity android:name=".FindingCircleActivity"/>
    </application>

</manifest>